using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Xunit;

namespace SimpleNetworking.Tests.Tests
{
    public class test
    {
        [Fact]
        public void testMethod()
        {
            //var bytes = BitConverter.GetBytes(DateTime.Now.ToBinary());
            //byte[] copy = bytes;
            //copy[copy.Length - 1] += 5; 
            //Assert.Equal(bytes, copy);

            //string myString = "åh vilken härlig dag......";
            //byte[] data = Encoding.ASCII.GetBytes(myString);
            
            //Assert.Equal(5, data.Length);
        }
    }
}
